import { ScreenWrapper } from "@siteed/design-system";
import { Text } from "react-native-paper";

const IndexPage = () => {
  return (
    <ScreenWrapper>
      <Text>ok index</Text>
    </ScreenWrapper>
  );
};
