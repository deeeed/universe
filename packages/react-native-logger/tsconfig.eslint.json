{
  "extends": "./tsconfig",
  "compilerOptions": {
    // ensure that nobody can accidentally use this config for a build
    "noEmit": true,
  },
  "include": [
    "./src",
    "./*.js",
    "./*.cjs",
    ".eslintrc.cjs",
  ],
  "exclude": ["dist/**", "node_modules", "coverage/**", "*.config.ts"]
}
