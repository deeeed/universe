import { Canvas, Meta, Controls } from '@storybook/blocks';
import { Story, ArgsTable } from '@storybook/addon-docs';

import * as Stories from './dyn-input.stories';
import { DynInput } from './dyn-input';

<Meta of={Stories} />

# Definition

`DynInput` is a versatile input handler for React applications, supporting various input types like text, textarea, number, radio, select-button, and custom render types. 
It provides a flexible interface for handling different forms of user input.


### Text Input

Use the `DynInput` for basic text input:
{/* 
<Canvas>
  <Story name="Text">
    <DynInput data="Hello World" inputType="text" />
  </Story>
</Canvas> */}
<Story of={Stories.Text} />
<Controls />
